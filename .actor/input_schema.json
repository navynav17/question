{
  "title": "CEE MCQ extractor input",
  "description": "Crawl all CEE chapter pages and extract questions, options, correct answers and explanations.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "description": "Pages where chapter links should be discovered.",
      "type": "array",
      "editor": "requestListSources",
      "items": {
        "type": "string",
        "description": "A page or direct MCQ URL to start from."
      },
      "default": [
        "https://pandeyramu.com.np/"
      ]
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "description": "Maximum number of pages processed concurrently.",
      "type": "integer",
      "default": 2,
      "minimum": 1,
      "maximum": 5
    },
    "maxRequests": {
      "title": "Max Requests",
      "description": "Maximum total pages visited in one run.",
      "type": "integer",
      "default": 500,
      "minimum": 1,
      "maximum": 5000
    },
    "username": {
      "title": "Fixed username",
      "description": "Name entered on every MCQ start screen.",
      "type": "string",
      "editor": "textfield",
      "default": "Apify Collector"
    },
    "testMode": {
      "title": "Test mode",
      "description": "When enabled, treat a direct /mcq/slug/ start URL as an MCQ test page and do not crawl the site hierarchy.",
      "type": "boolean",
      "editor": "checkbox",
      "default": false
    }
  },
  "required": []
}
