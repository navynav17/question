{
  "title": "CEE MCQ extractor input",
  "description": "Crawl all CEE chapter pages and extract questions, options, correct answers and explanations.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "description": "Pages where chapter links should be discovered.",
      "type": "array",
      "editor": "requestListSources",
      "default": [
        { "url": "https://pandeyramu.com.np/" }
      ]
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "description": "Maximum number of pages processed concurrently.",
      "type": "integer",
      "default": 2,
      "minimum": 1,
      "maximum": 5
    },
    "maxRequests": {
      "title": "Max Requests",
      "description": "Maximum total pages visited in one run.",
      "type": "integer",
      "default": 500,
      "minimum": 1,
      "maximum": 5000
    }
  },
  "required": []
}
