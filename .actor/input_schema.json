{
  "title": "CEE MCQ extractor input",
  "description": "Crawl CEE chapter pages and extract questions, options, correct answers and explanations.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start URLs",
      "description": "Pages where chapter links or a direct MCQ URL should be discovered.",
      "type": "array",
      "editor": "requestListSources",
      "default": [
        "https://pandeyramu.com.np/"
      ]
    },
    "maxConcurrency": {
      "title": "Max Concurrency",
      "description": "Maximum number of pages processed concurrently.",
      "type": "integer",
      "default": 1,
      "minimum": 1,
      "maximum": 5
    },
    "maxRequests": {
      "title": "Max Requests",
      "description": "Maximum total pages visited in one run.",
      "type": "integer",
      "default": 3000,
      "minimum": 1,
      "maximum": 5000
    },
    "username": {
      "title": "Fixed username",
      "description": "Name entered on every MCQ start screen. Use the same username for all tests.",
      "type": "string",
      "editor": "textfield",
      "default": "Abcdefgh"
    },
    "testMode": {
      "title": "Test mode",
      "description": "When enabled, process only the supplied direct MCQ URL.",
      "type": "boolean",
      "editor": "checkbox",
      "default": true
    }
  },
  "required": []
}